#ifndef FILE_H
#define FILE_H
#include <string>
#include <vector>
#include <wx/file.h>

class File : wxFile
{
public:
    void read(std::string& s,std::fstream& f);
    bool readFile(std::vector<std::string>& vec,const std::string& f);
    void writeFile(std::vector<std::string>& vec,const std::string& f);
    void write(const std::string& s,std::fstream& f);
};

#endif // FILE_H
